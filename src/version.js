// ─── APP VERSION ────────────────────────────────────────────────────────────
// Single source of truth for CanVault's version number, shown in Stats.
// Bump this at the end of every update session (see CLAUDE.md workflow):
//   - patch (1.0.x) for small fixes/tweaks
//   - minor (1.x.0) for new features
//   - major (x.0.0) for breaking/large changes
export const APP_VERSION = "1.0.3";
